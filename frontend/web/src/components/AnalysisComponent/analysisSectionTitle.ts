import { ANALYSIS_CARD_TITLE_CLASS } from '@/lib/pageTitle';

/** @alias — kart üst başlığı (Analiz) */
export const analysisSectionTitleClass = ANALYSIS_CARD_TITLE_CLASS;

export const analysisSectionSubtitleClass =
  'mt-1 text-[11px] leading-snug text-muted-foreground sm:text-xs';

/** Form / alt blok etiketi (küçük, kelime başı formatı JSX’te formatPageTitleDisplay ile) */
export const analysisLabelClass = 'text-xs font-medium leading-snug text-muted-foreground';
